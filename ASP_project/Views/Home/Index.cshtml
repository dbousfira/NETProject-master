@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Fresh Delivery</h1>
</div>

<div class="row">
    <% Html.RenderAction("Orders"); %>
    <% Html.RenderAction("Products"); %>
</div>